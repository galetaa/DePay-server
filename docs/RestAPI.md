# REST API

This file previously described planned endpoints that are not all present in the current repository.

For the factual API surface, use:

- `docs/api.md` for a human-readable endpoint summary;
- `docs/openAPI.yaml` for the OpenAPI contract.

The current coursework demo uses the real endpoints listed in those files.

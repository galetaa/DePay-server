# Postman collection status

The previous Postman collection described planned endpoints that are not part of the current coursework-ready API.

Use these factual API documents instead:

- `docs/api.md`
- `docs/openAPI.yaml`

If a Postman collection is needed later, generate it from `docs/openAPI.yaml` after the API contract is updated.

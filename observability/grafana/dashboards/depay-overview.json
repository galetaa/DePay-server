{
  "annotations": {
    "list": []
  },
  "editable": true,
  "panels": [
    {
      "datasource": {
        "type": "prometheus",
        "uid": "Prometheus"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "reqps"
        },
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 0
      },
      "id": 1,
      "targets": [
        {
          "expr": "sum by (service) (rate(depay_http_requests_total[5m]))",
          "legendFormat": "{{service}}"
        }
      ],
      "title": "HTTP request rate",
      "type": "timeseries"
    },
    {
      "datasource": {
        "type": "prometheus",
        "uid": "Prometheus"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "s"
        },
        "overrides": []
      },
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 0
      },
      "id": 2,
      "targets": [
        {
          "expr": "sum by (service) (rate(depay_http_request_duration_seconds_sum[5m])) / clamp_min(sum by (service) (rate(depay_http_requests_total[5m])), 0.001)",
          "legendFormat": "{{service}}"
        }
      ],
      "title": "Average HTTP duration",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": [
    "depay"
  ],
  "templating": {
    "list": []
  },
  "time": {
    "from": "now-30m",
    "to": "now"
  },
  "timezone": "browser",
  "title": "DePay Overview",
  "uid": "depay-overview",
  "version": 1
}
